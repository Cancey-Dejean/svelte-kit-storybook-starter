import Root from "./button.svelte";
import type {
	 ButtonProps,
	 ButtonSize,
	 ButtonVariant,
} from "./types";
import { buttonVariants } from "./variants";

export {
	Root,
	type ButtonProps as Props,
	Root as Button,
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
};
